{
  "name": "talon-channel-server",
  "version": "0.1.0",
  "private": true,
  "description": "Mattermost channel server for Claude Code (MCP stdio channel bridge)",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "probe": "tsx src/probe.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "ws": "^8.18.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/ws": "^8.18.1",
    "vitest": "^3.2.0"
  }
}
