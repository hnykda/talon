{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "talon.serviceAccountName" . }}
  labels:
    {{- include "talon.labels" . | nindent 4 }}
{{- end }}
